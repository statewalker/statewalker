{
  "name": "@statewalker/root",
  "private": true,
  "scripts": {
    "postinstall": "lerna bootstrap",
    "bootstrap": "lerna exec npm install",
    "test": "lerna exec npm test",
    "bundle": "lerna exec npm run bundle"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
